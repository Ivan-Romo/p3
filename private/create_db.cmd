sqlite3 users.db "CREATE TABLE games (game_id TEXT PRIMARY KEY, player1 TEXT, player2 TEXT); CREATE TABLE players (game_id TEXT, player_id TEXT, x INTEGER, y INTEGER, angle REAL, score INTEGER DEFAULT 0, bullets TEXT, PRIMARY KEY (game_id, player_id), FOREIGN KEY (game_id) REFERENCES games (game_id)); CREATE TABLE bullets (bullet_id TEXT PRIMARY KEY, game_id TEXT, player_id TEXT, x REAL, y REAL, direction REAL, created_at TIMESTAMP, FOREIGN KEY (game_id) REFERENCES games (game_id), FOREIGN KEY (player_id) REFERENCES players (player_id)); CREATE TABLE squares (game_id TEXT PRIMARY KEY, x INTEGER, y INTEGER, is_visible INTEGER DEFAULT 1, FOREIGN KEY (game_id) REFERENCES games (game_id)); CREATE TABLE game_collisions (game_id TEXT NOT NULL, bullet_id TEXT NOT NULL, timeCollide DOUBLE NOT NULL, player_id TEXT, latency INTEGER, PRIMARY KEY (game_id, bullet_id, timeCollide)); CREATE TABLE IF NOT EXISTS users (user_id INTEGER PRIMARY KEY, user_name VARCHAR(63), user_password VARCHAR(255)); CREATE UNIQUE INDEX user_name_UNIQUE ON users (user_name ASC);"
